# Contributor instructions

Scope: personal AI Chief of Staff and Organizer planning in this repository. Preserve the existing FastAPI/Shortcuts prototype and any more-specific instructions. No prior AGENTS.md existed in this checkout.

- **Current phase is documentation only:** create/update Markdown planning files. Do not implement code, install dependencies, download models, start services, alter personal files, create issues, commit or push without explicit authorization. The user has authorized publishing these planning documents through a local clone, commit and push. Later explicit build instructions authorize implementation within their stated scope.
- **Chief of Staff refinement:** drafting was restricted to local documentation; the subsequent user request explicitly authorizes committing and pushing this documentation revision. Application implementation remains unauthorized. Read the [Chief PRD](docs/chief-of-staff/PRD.md), [design](docs/chief-of-staff/DESIGN.md) and [plan](docs/chief-of-staff/IMPLEMENTATION_PLAN.md) for app-specific decisions.
- Read [docs/README.md](docs/README.md), [architecture](docs/SHARED_ARCHITECTURE.md) and [decisions](docs/DECISIONS.md) before changes. Distinguish verified code, vendor claims, assumptions, proposed targets and measured results.
- Future architecture: independent Python apps, app-owned SQLite state and small shared package. FastAPI/Jinja UI; one shared inference broker for both apps, local providers only, no silent cloud fallback. Code owns permissions, calculations, scheduling and all mutations; models supply schema- and meaning-validated proposals. No generated code execution or self-installing tools.
- Never commit private documents/photos, databases/WAL, logs, secrets, model weights, local configuration or real-data fixtures. Use synthetic data and temporary test roots. Check staged contents; `.gitignore` is not a privacy guarantee. Keep personal state outside the source tree.
- For future builds test deterministic operation without AI, schema/semantic rejection, joint-process inference limits, uncertain cancellation/crash recovery, browser Host/Origin/session/CSRF protections, and filesystem preview/approval/no-overwrite/path-escape recovery. No tests against personal Downloads or Photos libraries. Run checks appropriate to the changed behavior and report results accurately.
- Known read-only inspection commands: `rg --files`, `git -C <actual-repository> status --short`, `ollama --version`, `ollama list`, `ollama show llama3.2:latest`. Ollama queries assume an existing service; do not start one in this phase. This dedicated checkout is the Git repository; the original parent workspace and Organizer prototype are not. No build/test/run command exists yet for the proposed apps; add exact verified commands when packaging is implemented, not guessed launch instructions.
- Update stable requirement/acceptance links, decisions and milestone status with architectural changes. Record full model digests/runtime versions and label every performance number as a result or target. Preserve unrelated work; do not initialize Git across the parent multi-project workspace.
